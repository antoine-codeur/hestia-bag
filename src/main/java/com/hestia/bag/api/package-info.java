/**
 * Public API surface for HestiaBag, intended for use by other plugins that
 * want to extend or interoperate with the Hestia system (e.g. registering
 * custom upgrades, listening for home reset events).
 *
 * <p>Currently empty — the public API will be designed once the core plugin
 * stabilizes. Internal classes outside this package may be refactored at
 * any time.
 */
package com.hestia.bag.api;
